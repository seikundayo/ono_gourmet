<% if model.errors.any? %>
  <div class="alert alert-warning">
    <h4><%= pluralize(model.errors.count, "件") %> のエラーが確認されました。</h4>
    <ul>
      <% model.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
