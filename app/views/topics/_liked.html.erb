<% if user_signed_in? %>
  <%= link_to '❤️', topic_likes_path(topic), method: :delete, remote: true %>
<% elsif admin_signed_in? %>
  <%= link_to '❤️', topic_admin_likes_path(topic), method: :delete, remote: true %>
<% else %>
<% end %>
